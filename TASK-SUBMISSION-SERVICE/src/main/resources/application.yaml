server:
  port: 8082

spring:
  application:
    name: TASK-SUBMISSION-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/task_submission_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
